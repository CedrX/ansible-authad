[global]
   workgroup = {{ authad_workgroup }}
   client signing = yes
   client use spnego = yes
   kerberos method = secrets and keytab
   log file = /var/log/samba/%m.log
   password server = {{ authad_pdc }}
   realm = {{ authad_realm }}
   security = ads
